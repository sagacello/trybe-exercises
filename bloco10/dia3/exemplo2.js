const caixaAlta = (string) => string.toUpperCase();

const primeiraLetra = (string) => string[0]

const concatenaDuasStrings = (string1, string2) => string1.concat(string2)

console.log(concatenaDuasStrings('marcelo',' ivan'))

module.exports = {caixaAlta, primeiraLetra, concatenaDuasStrings}